[Unit]
Description=Eos — real estate photography SaaS
After=network.target
Wants=network-online.target

[Service]
Type=simple
User=eos
Group=eos
WorkingDirectory=/opt/eos/current
EnvironmentFile=/opt/eos/.env
Environment=EOS_CHECK_MODE=production
ExecStartPre=/opt/eos/current/.venv/bin/python /opt/eos/current/scripts/check-env.py
ExecStart=/opt/eos/current/.venv/bin/uvicorn eos.main:app --host 127.0.0.1 --port 8410 --workers 1
Restart=on-failure
RestartSec=5

NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/opt/eos/data /opt/eos/backups

[Install]
WantedBy=multi-user.target
